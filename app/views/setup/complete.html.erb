<% content_for :title, "Setup Complete — Ember Vault" %>
<div class="completion-page"><div class="completion-mark"><i></i><i></i><i></i></div><p class="section-index">CONFIGURATION SEALED</p><h1>The vault is<br>coming online.</h1><p><%= pluralize(@downloads.count, "resource") %> queued · <%= number_to_human_size(@configuration.projected_size_mb.megabytes) %> projected · AI: <%= @configuration.ai_profile.humanize %> · Theme: <%= @configuration.theme.humanize %></p><div><%= link_to "MONITOR DOWNLOADS", downloads_path, class: "button button-light" %><%= link_to "RETURN TO COMMAND", root_path, class: "button button-ghost" %></div></div>
